pipeline {
    agent any

    stages {
        stage('Ejecutar pruebas') {
            steps {
                script {
                    // Ejecutar las pruebas
                    sh 'python Prueba_test.py'
                }
            }
        }
    }
}
